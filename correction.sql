-- UPDATE visitor
-- SET first_date =  
-- substr(first_date, 7, 4) || '-' ||
-- substr(first_date, 4, 2) || '-' ||
-- substr(first_date, 1, 2) 
-- WHERE first_date Like '__.__.____';

-- SELECT * FROM visitor;

-- UPDATE article_giveaway
-- SET date =  
-- substr(date, 7, 4) || '-' ||
-- substr(date, 4, 2) || '-' ||
-- substr(date, 1, 2) 
-- WHERE date Like '__.__.____';

-- SELECT * FROM article_giveaway;

-- UPDATE service_use
-- SET date =  
-- substr(date, 7, 4) || '-' ||
-- substr(date, 4, 2) || '-' ||
-- substr(date, 1, 2) 
-- WHERE date Like '__.__.____';

-- SELECT * FROM service_use;

-- UPDATE workplan
-- SET date =  
-- substr(date, 7, 4) || '-' ||
-- substr(date, 4, 2) || '-' ||
-- substr(date, 1, 2) 
-- WHERE date Like '__.__.____';

-- SELECT * FROM workplan;